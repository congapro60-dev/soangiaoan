node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
__pycache__/
desktop.ini
out*.txt
temp*.html

bot_profile/

# ── Antigravity Tester Scripts (KHÔNG commit, chỉ chạy local) ──
bot_test.js
auto_learner.js
live_dom_test.js
qa_adaptive_test.js
full_e2e_test.js
e2e_giaoandewey.js
qa_script.js
login_setup.js
QA_REPORT.md
api_logs.zip
error_log.png

# ── Chrome profiles của Antigravity (KHÔNG commit) ──
bot_profile/
chrome-profile-qa/

# ── Thư mục con lồng nhau (embedded repo) ──
soangiaoan/

# ── Firebase Service Account Key (BÍ MẬT — KHÔNG BAO GIỜ COMMIT) ──
.agents/firebase-service-account.json
*firebase-adminsdk*.json

# ── File thừa/tạm (không cần commit) ──
_File thừa/

# Du lieu hoc sinh — KHONG BAO GIO commit (ho ten, email + mat khau mac dinh, phu huynh, dia chi).
# Dung wildcard vi ten thu muc tieng Viet luu dang NFD, go tay bang NFC se khong khop.
Danh s*ch l*p d*y*/

# ── Giáo án .docx do AI sinh (Manus/pipeline) — KHÔNG commit ──
# Đây là sản phẩm đầu ra, không phải mã nguồn. Hơn 700 file, đẩy lên sẽ phình repo
# và làm Vercel build chậm. Giữ ở local, chia sẻ qua Drive.
giao an manus tao/
giáo án định hướng/
manus huong dan/
.vercel

# Local brainstorming companion screens — never release artifacts.
.superpowers/
