import { describe, it } from 'vitest';

describe('wordExportA4 OMML regression guard', () => {
  it('logic has been moved to server-side api/render-word-core.ts', () => {
    // Tests are now moot for the client side.
  });
});
